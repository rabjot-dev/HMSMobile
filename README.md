# HMS Mobile

Patient mobile app for the Hospital Management System.

## Scripts

```bash
npm install
npm start
npm run android
npm run ios
npm run web
npm run lint
```

The app uses `src/constants/api.ts` for the backend API base URL.
